// @include "basil.js";// @include "./lib/underscore.js";function draw() {  var selectedItems = b.selections();  // only use first item selected  b.typo(selectedItems[0], "pointSize", 30);  // for-loop  for(var i = 0; i < selectedItems.length; i++){    b.typo(selectedItems[i], "pointSize", 30);  }  // using looping callback function  b.selections(function(item, i){    b.typo(item, "pointSize", 30);  });}b.go();